import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Yaqeen",
  description: "A comprehensive Islamic platform dedicated to providing authentic knowledge derived from the Holy Quran and the Sunnah. It delivers reliable and well-organized content in a simple and accessible way, designed to help users understand Islam correctly. The platform ensures an easy user experience, allowing access to trusted information anytime and anywhere, making Islamic knowledge closer, clearer, and more accessible to everyone.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
